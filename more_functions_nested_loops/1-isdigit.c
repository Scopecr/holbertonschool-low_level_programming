#include "main.h"
/**
 *_isdigit - identifies if there are digits on the program.
 *
 *@C: variable to identify as number
 *
 *Returns: 1 if digit found and 0 if no digit
*/
int  _isdigit(int c)
{
	if (c >= 0 && c <= 10)

	{

		return (1);

	}

	else
	{

		return (0);

	}
}
