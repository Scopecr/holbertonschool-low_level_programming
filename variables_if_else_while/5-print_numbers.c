#include <stdio.h>
/**
 * main - starting point
 *
 * char n - indicates the increase in value
 *
 * Return: always 0 (Success)
 */

int main(void)
{
	char n;

	for (n == 0; n < 10; n++)
	{
		putchar(n);
	}

	putchar('\n');

	return (0);
}
