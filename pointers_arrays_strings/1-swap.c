#include "main.h"
/**
 *swap_int - swaps the value of two intergers
 *
 * @a: interger to swap with b
 * @b: ineterger to swap with a
 *
 * Return: void
 */

void swap_int(int *a, int *b)
{
	int temp;

	temp = *a;
	*a = *b;
	*b = aux;
}
